#!/usr/bin/env fennel

(local tests [(require :tests/lexer)
              (require :tests/tokenizer)
              (require :tests/builtins)
              (require :tests/parser)])

(each [index test (ipairs tests)]
  (if (not (= (type test) :string))
    (test.run)))
